import{_ as i,c as s,o as t,ag as n}from"./chunks/framework.CA4hJK0u.js";const k=JSON.parse('{"title":"Agent Identity and OBO","description":"","frontmatter":{},"headers":[],"relativePath":"agent_identity.md","filePath":"agent_identity.md","lastUpdated":1785591251000}'),a={name:"agent_identity.md"};function o(l,e,r,d,h,p){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="agent-identity-and-obo" tabindex="-1">Agent Identity and OBO <a class="header-anchor" href="#agent-identity-and-obo" aria-label="Permalink to &quot;Agent Identity and OBO&quot;">​</a></h1><p>AgentKit Runtime can authenticate a signed-in user, bind the request to the Runtime&#39;s own Workload Identity, and obtain a short-lived, target-bound workload access token (TIP) before calling a protected downstream service.</p><p>This API is experimental in the 0.8.x line. The credential safety kernel lives in the framework-neutral <code>agentkit_identity</code> source package, which is bundled in and versioned with the same <code>agentkit-sdk-python</code> wheel. It is a separate code module and dependency boundary, not a separately published artifact.</p><p>The core module is present in a normal AgentKit installation. Install the <code>identity-runtime</code> extra when using the Google ADK-based AgentServer integration:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;agentkit-sdk-python[identity-runtime]&quot;</span></span></code></pre></div><p>The core is a data-plane SDK. It is deliberately separate from <code>agentkit.sdk.identity</code>, which configures Runtime inbound authorizers through the management plane.</p><p>The independent <code>agentkit_identity</code> package owns OIDC/TIP verification, the private request context and lease, OBO coordination, credential scrubbing and target-bound transport. It does not import AgentKit, Google ADK or OpenClaw. AgentKit retains its route/session binding policy, Runtime IAM credential resolution, Identity OpenAPI adapter, AgentServer composition and CLI login session. A missing or incompatible bundled module fails startup when Identity is configured; ordinary AgentKit use with <code>identity=None</code> does not eagerly load the module.</p><h2 id="trust-model" tabindex="-1">Trust model <a class="header-anchor" href="#trust-model" aria-label="Permalink to &quot;Trust model&quot;">​</a></h2><p>The deployment, not an inbound request, provides an immutable <code>IdentityRuntimeConfig</code>:</p><ul><li><code>runtime_id</code> is also the Workload Identity name in this version;</li><li>the expected OIDC issuer, discovery URL, explicit JWKS origins, and allowed client IDs define which user ID Tokens are trusted;</li><li>the Workload Pool discovery URL pins the issuer/JWKS used to verify returned TIPs;</li><li>every downstream alias maps to exactly one audience and, for HTTP, one HTTPS base URL;</li><li>Runtime IAM credentials authorize the call to <code>GetWorkloadAccessTokenForJWT</code>.</li></ul><p>For the first APIG integration, the gateway or deployment configuration can keep the trusted <code>route/runtime -&gt; workload -&gt; audience</code> registry. A future control plane can construct the same configuration object; callers must never supply or override this binding in a request.</p><p>V1 therefore expects a pre-created Runtime/Workload and deployment-injected configuration. It does not infer a trusted Runtime ID from Agent business code.</p><h2 id="runtime-integration" tabindex="-1">Runtime integration <a class="header-anchor" href="#runtime-integration" aria-label="Permalink to &quot;Runtime integration&quot;">​</a></h2><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> agentkit.apps </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> AgentkitAgentServerApp</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> agentkit.identity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> IdentityClient</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> agentkit_identity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    IdentityRuntimeConfig,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ProtectedTarget,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RuntimeIdentity,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">identity </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RuntimeIdentity(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    IdentityRuntimeConfig(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        runtime_id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;runtime-123&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># also the Workload Identity name</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        workload_pool</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        discovery_url</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;https://userpool-example.userpool.auth.id.cn-beijing.&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;volces.com&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ),</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        expected_user_issuer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;https://userpool-example.userpool.auth.id.cn-beijing.&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;volces.com&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ),</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        allowed_clients</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;agentkit-public-client&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,),</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        workload_discovery_url</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;https://auth.id.cn-beijing.volces.com/workloadpool/&lt;pool-id&gt;/&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;.well-known/openid-configuration&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ),</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        expected_workload_issuer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;https://auth.id.cn-beijing.volces.com/workloadpool/&lt;pool-id&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ),</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        targets</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &quot;expense&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: ProtectedTarget(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">                alias</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;expense&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">                audience</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;trn:customer:expense-api&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">                base_url</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://gateway.example.com/expense&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            )</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Direct exchange is explicit. Use it only after the Identity service has</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # an authoritative Runtime-IAM -&gt; Workload binding for this deployment.</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    exchange</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">IdentityClient(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">region</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cn-beijing&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> AgentkitAgentServerApp(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    agent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">agent,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    short_term_memory</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">memory,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    identity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">identity,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> identity.authorized_session()</span></span></code></pre></div><p><code>agentkit.identity</code> continues to re-export the public runtime objects for the 0.8.x compatibility period, and those aliases are the same class objects as the canonical <code>agentkit_identity</code> imports. New code should use the canonical package for Runtime primitives and import only AgentKit-owned adapters such as <code>IdentityClient</code> from <code>agentkit.identity</code>.</p><p><code>AgentIdentityMiddleware</code> is the AgentKit route-policy adapter installed by <code>AgentkitAgentServerApp</code>. The underlying <code>IdentityASGIMiddleware</code> and private credential lifecycle stay in the independent source package within the same wheel. Credential-free browser <code>OPTIONS</code> preflight is delegated to the inner CORS middleware or router. Before any authenticated Agent or Tool route runs, the combined enforcement path:</p><ol><li>reads exactly one inbound Bearer ID Token;</li><li>verifies signature, algorithm, issuer, audience/client, expiry, issued-at, subject, and the OIDC authorized party rule;</li><li>removes the raw <code>Authorization</code> header from the child ASGI scope;</li><li>binds an immutable <code>IdentityContext</code> for the whole streaming request;</li><li>uses the verified <code>sub</code>, not caller-provided <code>user_id</code>, on AgentKit&#39;s <code>/invoke</code> and <code>/run_sse</code> paths;</li><li>fails with a server error if Identity mode is enabled but the verified context is missing, instead of falling back to the request body or headers.</li></ol><p>The binding includes a revocable request lease. A detached <code>asyncio</code> task may inherit the Python context, but it cannot start a new protected operation after revocation. An operation admitted before revocation holds the lease from TIP acquisition through downstream request completion; normal return and request cancellation both drain those operations before the ASGI request completes.</p><p>ADK session paths under <code>/apps/{app}/users/{user}/...</code> are rejected unless the path user equals the verified subject. The unbound ADK <code>/run</code> route and A2A mount are disabled in identity mode in V1; use <code>/invoke</code> or <code>/run_sse</code>. Identity mode also rejects WebSocket, debug, eval, administrative, and future unknown routes until they have an explicit subject-binding rule.</p><p>Verification is SDK middleware. It does not belong in <code>agent.py</code>.</p><h2 id="calling-a-protected-target" tabindex="-1">Calling a protected target <a class="header-anchor" href="#calling-a-protected-target" aria-label="Permalink to &quot;Calling a protected target&quot;">​</a></h2><p>Agent or Tool code calls a registered alias, not an arbitrary host:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">response </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> http.request(</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;GET&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;expense&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;/v1/expenses/123&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    timeout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">response.raise_for_status()</span></span></code></pre></div><p>For training and observability, callers can request token-free evidence after the same exchange and validation path succeeds:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">receipt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> identity.delegation_receipt(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;expense&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># DelegationReceipt(target_alias=&quot;expense&quot;, subject=&quot;alice&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#                   actor=&quot;runtime-123&quot;, audience=&quot;...&quot;, expires_at=...)</span></span></code></pre></div><p>The receipt never contains the ID Token or TIP and is not an authorization credential. Protected business calls must still use <code>AuthorizedSession</code>.</p><p>The SDK exchanges the verified user ID Token with <code>GetWorkloadAccessTokenForJWT</code>, requesting the configured audience. It rejects a response whose <code>sub</code>, direct <code>act.sub</code>, <code>aud</code>, or lifetime does not match the request. It also verifies the TIP signature and issuer against the configured Workload Pool discovery document, prevents a TIP from outliving the delegated user token, caches it in a bounded in-memory cache keyed by the subject-token fingerprint/user/Runtime/target, injects it as the downstream Bearer token, and disables redirects. Business code cannot override the <code>Authorization</code> header or target host through this transport. It also rejects routing/forwarding headers, caller cookies, <code>TRACE</code>/<code>CONNECT</code>, response cookies, and security-sensitive <code>requests</code> options. The returned response and any raised SDK error do not retain the prepared request or raw connection that carried the TIP. V1 buffers the response body before releasing the request lease, rejects caller-selected streaming, and rejects configured Requests timeout values above 300 seconds.</p><p>JWKS document caching has an explicit bounded TTL (300 seconds by default), and resolved signing keys are not cached indefinitely by <code>kid</code>. Same-<code>kid</code> key replacement therefore takes effect no later than the configured JWKS TTL.</p><p>The downstream service or APIG independently verifies the TIP and audience and enforces policy over the user (<code>sub</code>), Agent/Workload (<code>act.sub</code>), action, and resource.</p><p><code>RuntimeIdentity</code> intentionally has no implicit exchange for protected targets. Before the Identity control plane exposes an authoritative Runtime-to-Workload binding, the demo deployment must inject a trusted <code>WorkloadTokenExchange</code> implemented by APIG or a deployment-owned adapter. APIG keeps the immutable <code>route/runtime -&gt; workload -&gt; audience</code> registry, authenticates the Runtime, performs the exchange, and returns the TIP for SDK verification. This temporary path does not put binding logic in <code>agent.py</code> and can later be replaced by the control-plane binding without changing Agent or Tool APIs.</p><h2 id="cli-login-flow" tabindex="-1">CLI login flow <a class="header-anchor" href="#cli-login-flow" aria-label="Permalink to &quot;CLI login flow&quot;">​</a></h2><p>The browser login uses Authorization Code with PKCE and OIDC nonce validation. AgentKit verifies the returned ID Token before saving the session. A cached, still-valid ID Token can be read without opening the OS keychain; the refresh token is resolved only when refresh is actually required.</p><p>For a direct JWT-authorized Runtime endpoint:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">agentkit</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> invoke</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --endpoint</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://runtime.example.com</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --use-login</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;hello&quot;</span></span></code></pre></div><p><code>--use-login</code> is explicit and cannot be combined with an API key or a manually supplied <code>Authorization</code> header. It requires HTTPS, except for an explicit loopback HTTP endpoint. Transport credentials are stripped when the CLI or Runner converts a request into A2A business metadata.</p><h2 id="security-boundary-and-current-limitations" tabindex="-1">Security boundary and current limitations <a class="header-anchor" href="#security-boundary-and-current-limitations" aria-label="Permalink to &quot;Security boundary and current limitations&quot;">​</a></h2><ul><li><code>runtime_id == workload_id</code> is the current product contract, but string equality inside Python is not proof of deployment ownership. Runtime IAM plus the Identity service, and later the APIG/control-plane registry, must enforce that ownership server-side.</li><li>The repository includes <code>IdentityClient</code> for direct Identity OpenAPI exchange, but the host/service/version/action still require a live compatibility check. Do not use it where the Identity service has not yet enforced Runtime IAM to Workload ownership; inject an APIG-bound exchange instead.</li><li><code>IdentityClient</code> is currently Volcengine-only and pins the Volcengine Agent Identity endpoint even if the process-wide AgentKit provider is BytePlus.</li><li>Token redaction and private Python attributes reduce accidental exposure; they do not isolate secrets from malicious code in the same process. A gateway, sidecar, or separate broker is required for that stronger threat model.</li><li>A TIP is still a short-lived bearer token. V1 validates its subject, direct actor, singleton audience, issuer, and lifetime, but does not provide DPoP, sender-constrained proof, or a consumed-<code>jti</code> replay ledger.</li><li>Except for credential-free CORS preflight, the SDK authenticates every HTTP route when identity mode is enabled. V1 rejects mismatched ADK session paths and disables the unbound <code>/run</code> and A2A paths.</li><li>The refresh token prefers the OS keychain. Existing compatibility behavior can fall back to a <code>0600</code> session file when no usable keychain exists; deployments that prohibit file-held refresh tokens must enforce a keychain-only policy.</li><li>Saved sessions created before verified <code>user_sub</code> binding continue using a still-valid cached ID Token, but require one new browser login when refresh is next needed. This is a deliberate fail-closed upgrade behavior.</li><li>Standard OIDC deployments may advertise a JWKS endpoint on an origin different from discovery. AgentKit requires an exact deployment-configured issuer match; a cross-origin JWKS endpoint must also be listed explicitly in the trusted JWKS-origin allowlist. Each individual request still blocks cross-origin redirects.</li><li>The configured JWKS cache TTL is also the maximum normal signing-key revocation delay; V1 has no emergency process-wide cache purge API.</li><li>The SDK validates HTTPS origins and redirects but does not itself reject every private, link-local, metadata, or DNS-rebinding destination. Production deployments must pin expected endpoints and enforce an egress allowlist.</li><li>The Identity OpenAPI action, signing service, endpoint, and version in this implementation follow the repository&#39;s fixed API baseline. They require a live compatibility check before a production release.</li></ul>`,37)])])}const u=i(a,[["render",o]]);export{k as __pageData,u as default};
